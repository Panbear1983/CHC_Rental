# CHC_Rental — environment variable placeholders only.
# Copy to .env and fill in real values locally. Never commit .env.
# Phase 1 does not read or use any of these; they are placeholders for later phases.

# Telegram bot token used by a future OUTBOUND-ONLY notifier (Phase 5+).
# No inbound poller/handler exists in this codebase.
TELEGRAM_BOT_TOKEN=changeme

# Path to the local SQLite database file.
CHC_RENTAL_DB_PATH=data/chc_rental.sqlite3

# Licensed RentCast rental-listing source. It is inert unless BOTH the explicit
# enabled flag and a positive one-process request budget are supplied.
# The adapter exposes a config-only health check and requires an injected HTTP
# transport; it does not schedule, deliver, or make network calls by default.
RENTCAST_API_KEY=changeme
RENTCAST_ENABLED=false
RENTCAST_MAX_REQUESTS=0
